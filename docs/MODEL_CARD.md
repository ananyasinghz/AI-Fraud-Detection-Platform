# Model Card

**Status:** Placeholder — populated in Phase 1

No first-party model exists in Phase 0.

Phase 1 must document:

- intended use and prohibited claims
- training, validation, and test sources
- duplicate-safe split method
- preprocessing and feature schema
- model candidates and selected model
- validation-only threshold selection
- untouched test metrics and alert rate
- calibration status
- known limitations and subgroup/sensitivity checks
- artifact/version fingerprints

The imported third-party model is not the production model and its reported metrics are not this project's results.
